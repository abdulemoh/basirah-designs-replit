{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "membership_status": {
      "type": "string",
      "title": "Membership Status",
      "enum": [
        "active",
        "cancelled"
      ],
      "description": "Whether the user has an active Standard Plan membership"
    },
    "build_fee_paid": {
      "type": "boolean",
      "title": "Build Fee Paid",
      "default": false,
      "description": "Whether the user has paid the one-time $1,000 build fee"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}